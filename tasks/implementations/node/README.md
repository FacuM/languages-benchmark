# node benchmark implementation

This directory contains the shared JavaScript benchmark task implementations for
Node.js-compatible backend runtimes.

The same task sources are executed by:
- Node.js
- Bun
- Deno (using Node compatibility plus CommonJS project detection)
